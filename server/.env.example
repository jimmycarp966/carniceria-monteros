# Puerto y entorno
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=tu_secreto_jwt_super_seguro

# Firebase Admin - usa SOLO UNA de estas opciones
# 1) JSON inline (contenido del service account completo)
GOOGLE_APPLICATION_CREDENTIALS_JSON=
# 2) JSON en base64
GOOGLE_APPLICATION_CREDENTIALS_B64=
# 3) Ruta a archivo JSON (por ejemplo server/credentials/service-account.json)
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_APPLICATION_CREDENTIALS_PATH=


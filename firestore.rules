rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas temporales para desarrollo - permitir todo
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 